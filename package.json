{
  "name": "evotempus",
  "version": "0.3.0",
  "scripts": {
    "build": "cd app && yarn build:prod",
    "init-app": "cd app && yarn install",
    "init-server": "cd backend && npm install",
    "init": "npm run init-app && npm run init-server",
    "heroku-postbuild": "npm run init && npm run build",
    "start": "./production/run-prod-local.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/phantomjinx/evotempus.git"
  },
  "author": "phantomjinx",
  "license": "GPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/phantomjinx/evotempus/issues"
  },
  "homepage": "https://github.com/phantomjinx/evotempus#readme"
}
